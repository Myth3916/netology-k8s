apiVersion: v1
kind: Service
metadata:
  name: multi-app-service
spec:
  selector:
    app: multi-app
  ports:
    - name: nginx-port
      port: 80
      targetPort: 80
    - name: multitool-port
      port: 8080
      targetPort: 8080
